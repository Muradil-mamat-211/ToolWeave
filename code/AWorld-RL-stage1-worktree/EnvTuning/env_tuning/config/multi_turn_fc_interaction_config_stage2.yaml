interaction:
  - class_name: "env_tuning.interaction.new_multi_turn_fc.MultiTurnFunctionCallInteraction"
    config:
      name: "multi_turn_tool_call_stage2"
      # Stage 2: standard (non-augmented) environment. is_augmented is EXPLICIT.
      environment_feedback_mode: "standard"
      is_augmented: false
