schema_version: toolweave.machine.v1
machine:
  source_root: ${TOOLWEAVE_SOURCE_ROOT}
  asset_root: ${TOOLWEAVE_ASSET_ROOT}
  models_root: ${TOOLWEAVE_MODELS_ROOT}
  data_root: ${TOOLWEAVE_DATA_ROOT}
  shared_data_root: ${TOOLWEAVE_SHARED_DATA_ROOT}
  artifacts_root: ${TOOLWEAVE_ARTIFACTS_ROOT}
  outputs_root: ${TOOLWEAVE_OUTPUTS_ROOT}
  logs_root: ${TOOLWEAVE_LOGS_ROOT}
  reports_root: ${TOOLWEAVE_REPORTS_ROOT}
  evals_root: ${TOOLWEAVE_EVALS_ROOT}
  cache_root: ${TOOLWEAVE_CACHE_ROOT}
  temp_root: ${TOOLWEAVE_TEMP_ROOT}
  short_temp_root: ${TOOLWEAVE_SHORT_TEMP_ROOT}
  python_executable: ${TOOLWEAVE_PYTHON}
  synthesis_python_executable: ${TOOLWEAVE_SYNTH_PYTHON}
  conda_environment: ${TOOLWEAVE_CONDA_ENV}
